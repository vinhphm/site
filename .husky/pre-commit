if git diff --cached --name-only | grep -q 'src/assets/fonts/.*\.woff2'; then
  bun subset-fonts
  git add src/assets/fonts/
fi

bun x oxlint --fix .
bun x oxfmt --no-error-on-unmatched-pattern .
bun x prettier --write "**/*.astro"
git add -u
