if git diff --cached --name-only | grep -q 'src/assets/fonts/.*\.woff2'; then
  bun subset-fonts
  git add src/assets/fonts/
fi

npx oxlint --fix .
npx oxfmt --no-error-on-unmatched-pattern .
npx prettier --write "**/*.astro"
git add -u
